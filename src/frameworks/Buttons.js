const { ButtonInteraction } = require('discord.js');

module.exports = {
  name: '',
  
  /**
   * Executes a button
   * @param {ButtonInteraction} interaction
   * @param {import('./SkyHelper.js')} client 
   */
  async execute(interaction, client) {
  }
}